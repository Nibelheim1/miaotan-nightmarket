# 测试报告 · 喵弹夜市 1.0.0

执行日期：2026-09-06。当前生产构建：**114,327 字节**，9 个源模块合成单文件，运行时零第三方依赖。

SHA-256：`2a37a160eaecceeffc8dc8a54a3bc4f24015ad82c96e403b7c22227b8baa8580`

## 结论与范围

**构建通过；63 项 Node 自动测试通过；107 项 Chromium 浏览器检查通过；600 次模式／策略脚本对局执行完毕。** HTTP 预览服务的 4 项独立检查通过。没有发现本次已覆盖路径中的未修复阻塞性错误。

这不等于 TapTap 审核通过、真实手机兼容全部通过、真人喜欢这款游戏或具有某个确定爆款概率。生产代码可玩；账号、平台 SDK 和公开部署没有执行。

## 1. 工具与加载方式

Node.js v22.16.0，npm 10.9.2，Python Playwright，Linux Chromium 144.0.7559.96。已实际执行安装、构建、Node 测试、本地服务、生产 HTML 浏览器运行及截图。

环境管理员禁止了 Chromium 的 HTTP 与 file URL 导航，返回 `ERR_BLOCKED_BY_ADMINISTRATOR`。因此：

- HTTP 服务用程序真实请求验证：状态码 200、正确 HTML MIME、返回内容与构建逐字节一致、404 路径；不是仅看终端提示。
- 浏览器用 `page.set_content` 加载生产单文件 HTML，实际运行 Canvas、JavaScript、DOM、鼠标／触屏事件、音频解锁代码和 PNG 导出。
- 第一发单独使用**未经诊断修改的生产 HTML**检查，触屏发射后正常进入三选一；该页面没有调试对象。
- 其他回归启用已有 `debug=1&seed=42` 入口，部分等待使用固定步加速，未直接修改正常完整局的生命、分数或敌人数据。压力场景才是特意注入的满负载场地。
- `about:blank` 不提供可用的原生 LocalStorage。存档跨文档测试使用明确标注的 Storage-compatible 测试替身，另测了真正不可用时的警告与内存回退。**没有把这个结果当成真实站点的原生持久化验证。**

浏览器完整回归耗时 18.75 秒，因为长段弹珠等待使用了诊断固定步加速。不能把这个时间当游戏实际平均局长。报告原始文件：`evidence/browser-results.json`。

## 2. Node 自动测试：63 / 63

命令：`npm test`；原始 TAP：`evidence/unit-tests.tap`。

覆盖 PRNG 确定性与极端种子、UTC+8 日期切换、首发／首次改装、NaN／Infinity 输入、连续点击、无操作、固定步碰撞、侧墙／顶部／底部反弹、穿透、分裂不递归、冰糖与传导、整行攻击、破甲、护盾、猎手标记、爆炸去重、鱼罐、Boss 恢复、实体／事件／队列上限、11 秒回收、普通漏怪／Boss 越线、双格阶段、最后清场、改装池耗尽、换牌与跳过、配方、每日固定店长、存档回放、非法存档与原型键、阻止存档／配额／损坏 JSON、局外奖励、榜单筛选、事件上限、音频未手势前不创建、200 次重开、长局与满改装压力。

最后一项回归新增验证：损坏 JSON 不误报为浏览器拒绝存储；后续成功写入能清除先前的失败状态。

## 3. 浏览器检查：107 / 107

尺寸：320×568、360×640、390×844、412×915、480×960、768×1024、1280×900、844×390；DPR 1／2／3 的不同组合，渲染内部上限 2。

在这些尺寸实际检查主页溢出、主按钮可达、Canvas 与底部条不越界、触屏一次合法发射、改装弹窗与选择后的继续。另检查声音与减少震动开关、帮助、无操作、pointercancel、快速连点、暂停、后台隐藏、回来等待确认、窗口失焦、游玩中横竖屏 resize、回主页、恢复上局、鼠标打完整局、PNG 分享、同种子重开、键盘、主动结束、失败重开、12 次快速重开、今日同题、本机榜、无尽解锁、不可用与损坏存档降级。

未捕获的页面 JS 错误：0。游戏发起的外部 HTTP 请求：0。浏览器系统分享没有可用的真实目标应用；验证的是 PNG 生成与降级入口，不是已发送到社交平台。

### 完整 UI 对局结果

种子 42，薄荷店长，实际鼠标选择角度、按钮选择改装，期间保存并恢复过上局；最终获胜。

| 指标 | 实际记录 |
|---|---:|
| 发射次数 | 19 |
| 结束波次 | 19（第 18 波后清场） |
| 分数 | 27,917 |
| 最高连击 | 166 |
| 物理飞行累计秒数 | 73.61 |

这是一局自动化交互验证，不是独立真人试玩样本。结算与分享卡的墙钟时长受加速测试影响，不能拿作营销中的普通玩家局长。

## 4. 数值代理：450 次普通模式＋150 次额外模式

命令：`npm run simulate`、`node tests/extended-simulation.mjs`。种子 1—75 在不同策略／模式间复用；**不是 600 位玩家、不是 600 个独立随机实验**。瞄准／抽牌代理不同于人类，模型没有声称拟合新手行为。

普通模式各策略 75 次；飞行时间排除读牌、瞄准、UI 和暂停。

| 瞄准／选牌策略 | 完成对局 | 获胜 | 飞行均值（秒） | 飞行 P95（秒） |
|---|---:|---:|---:|---:|
| random/random | 75/75 | 71/75 | 86.82 | 115.03 |
| center/balanced | 75/75 | 11/75 | 39.74 | 100.11 |
| priority/balanced | 75/75 | 73/75 | 74.88 | 95.22 |
| bank/balanced | 75/75 | 74/75 | 77.92 | 95.73 |
| priority/explosion | 75/75 | 70/75 | 83.43 | 112.94 |
| priority/ricochet | 75/75 | 74/75 | 83.72 | 114.30 |

每日同题逻辑用 75 个显式种子测试，全部自然结束、73 次获胜；是逻辑覆盖，不是跨 75 天的实际用户统计。

无尽模式 75 次，全部自然失败而结束；到达波次 21—117，平均 65.51 波，平均飞行 409.88 秒。120 发射测试截断上限未截断本批次。没有把无尽模式“0 次获胜”记作软件错误。

当前版本的普通模式对随机瞄准仍然宽容：71/75 获胜；直线代理仅 11/75。可以据此知道角度与借墙有作用，但不能据此宣称人类新手胜率、无法刷分、不存在最优构筑或长期平衡已经证明。保卫战保留轻松通关导向，无尽承担后续挑战。

原始轨迹摘要：`evidence/balance-results.json`、`evidence/extended-simulation.json`。

## 5. 压力与帧时间

浏览器额外注入高血量、多排怪和全机制的人工压力场景，运行并捕捉真实 Canvas。采样最后 110 帧，中位帧间隔 16.7ms、P95 16.7ms；采样时 110 颗弹珠、208 个粒子、64 个短时效果、264 个 DOM 节点。

这是共享主机无头 Chromium 的短时结果，不是中端手机 60 FPS 保证，也不是长时间温度／电量评估。它验证了预算没有失控。纯逻辑模拟中同屏最大弹珠达到硬上限 112，未突破。

## 6. 三轮审查和实际修订

| 审查 | 发现的问题 | 真正落实的修改与证据 |
|---|---|---|
| Review 1：玩法与节奏 | 初版随机代理几乎全通；同方向机械发射差异不明显；无尽线性敌人成长被玩家成长抵消 | 普通 HP 斜率从 1.45 试调到 4、Boss 系数从 8 到 20；第 13 波双格推进并显式预告；无尽增加非线性压力。保留 `review1-balance-before.json`、`review1-balance-mid.json` 与最终 600 次数据。这些是探索性调优，不是严谨单变量 A/B |
| Review 2：产品体验 | 不可存档提示可能压住底部操作；改装弹窗文案像要求再开一局；初始怪物会不必要闪白；发射点移到边缘遮挡装饰字；分裂穿透文字与规则有出入 | 警告条单独预留高度；改为“加点料，继续开摊”；初始 hit=-100；边缘时隐去会碰撞的装饰字；同步穿透描述；8 种尺寸截图和真实触屏复测 |
| Review 3：工程与边界 | 初始化声音设置曾可能提前创建音频上下文；同种子重开日志与真实 seed 不一致；几乎耗尽的改装池少于三项；非法原型键可能被误认为改装；损坏 JSON 被误当权限失败 | 将 AudioContext 创建留到交互；seedOverride 在初始化前统一使用；补给补齐三项；升级字典用无原型对象并加强存档整数检查；分离解析失败和权限失败。63 个 Node 测试全部通过，107 个浏览器检查全部复测 |

## 7. 尚未执行／已知边界

真实 iPhone Safari、Android WebView／TapTap 容器、低端手机长时发热、电量、真实网址导航与原生持久化、上线审核、原生应用打包、平台登录／广告／支付／全局榜单接入、多人试玩、次留和中长期付费数据：均未完成或未测，不用模拟结果替代。

本机存档可改写，刷新回到轮前允许重试，不具有服务端反作弊能力。当前不含云存档。完整度是**本地 H5 游戏循环与交付文件的完整度**，不是这些未接入商业服务已完成。

## 8. 重现与包体复核

```bash
npm install
npm run build
npm test
npm run simulate
node tests/extended-simulation.mjs
node tools/check-http.mjs
python tests/browser_test.py
```

最终压缩包另作 CRC、重新解压、干净目录安装／构建／测试及 SHA-256 对比，结果见 `evidence/release-validation.json`。`SHA256SUMS.txt` 覆盖交付目录中其余文件，不包含自身。浏览器自动化工具为可选本地安装，不打包其执行程序或字体。
