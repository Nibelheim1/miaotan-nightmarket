# 喵弹夜市 · 设计说明

## 核心承诺

“只松开一次手，整条街一起开花。”一指瞄准的确定性感，与升级后不可完全预料的连锁组合。可爱夜市取代重资产地牢；用"全清才过波"的打烊压力和不断前进的阵型提供紧张感，不用倒计时催操作。每一轮弹射都有明确代价：清不干净就扣心，漏掉的怪还会回来。

核心循环：选择角度 → 连续弹射与绕背 → 击破、连击与资源 → **全清本波才过波** → 每若干波三选一 → 敌人前进 → 守住 18 波／失败 → 结算印章与配方 → 换店长或换种子重开。

## 首次体验与局长

无需注册、下载资产或读说明书。主页主按钮开始；第一屏直接画出瞄准方向与"小炸弹"提示。首发沿引导方向真实击破 5 个敌人、清空第 1 波，物理模拟约 2 秒。没有伪演出或自动给分。首次三选一在首次斩杀大王（第 6 波）后出现。

普通局的设计目标为 2—4 分钟，包括瞄准和读改装；这不是已测真人均值。自动代理的完整普通局物理演出多数约 1—2 分钟，不包含思考、选择和暂停。无尽模式刻意允许更长。

## 当前数值与危险模型

全部参数集中在 `src/data/config.js`：基础参数在 `CONFIG`，六关修正表在 `LEVELS`（引擎运行时合并为 `game.L`）。保卫战 = 6 个线性解锁的关卡，每关一场完整波次战役；每日同题与无尽固定使用第 2 关规则。第 1 关强制薄荷店长（教学一致性）。

| 关卡 | 波数 | 生命 | 命中预算 | 危险线 | 关键变化 |
|---|---|---:|---:|---:|---|
| 1 开张大吉 | 12 | 5 | 无 | 487 | 惩罚不递增、前 4 波保底炸弹、纯横排教学 |
| 2 夜市常客 | 18 | 6 | 14 | 487 | 标准关（每日同题/无尽共用此规则） |
| 3 周末人潮 | 18 | 6 | 8 | 487 | 每波 +1 敌、棋盘阵、铁皮概率升、夜宵潮提前到 11 波 |
| 4 雨夜急单 | 18 | 5 | 7 | 460 | 危险线上移、竖列阵、大王血 ×7 |
| 5 大王连任 | 18 | 6 | 7 | 460 | 大王每 5 波、护卫 3 只、可双王同场 |
| 6 通宵达旦 | 24 | 6 | 8 | 440 | 惩罚 ×1.5 递增、广告 2 次、重复命中衰减 ×0.85 下限 40% |

通用基础规则（全关共享）：

| 参数 | 当前规则 |
|---|---|
| 场地 | 420×580 逻辑像素；7 列；每格 52；普通怪 46×46 |
| 初始生命 | 见关卡表，最大 8；普通漏怪先扣盾，否则扣 1 心 |
| 过波条件 | 全清即过波；未全清扣心（**连续失败递增**，倍率见关卡表，护盾不挡），残敌保留血量并入下一波；最后一波没有下一波，必须清完才算赢 |
| 越线规则 | 普通怪越线扣 1 心（护盾可挡）并**逃回街尾重新排队**，仍须消灭；漏怪永远不算清完 |
| Boss | 节奏见关卡表 bossEvery（末波必有大王）；横向 2 格；击杀触发免费三选一（当轮免惩罚），未消灭会随残敌压入下一波；越线直接失败；击破回 1 心 |
| 波次推进 | 每轮弹射后波次 +1 并刷新（除非最后一波未清完）；残敌每轮前进 1 格，夜宵潮起 2 格；进入夜宵潮时提示 |
| 阵型 | 1—6 波单横排；中段阵型按关卡（双簇／棋盘／竖列）；夜宵潮起双排交错 |
| 命中预算 | 2 关起每颗弹珠每轮命中次数封顶（见关卡表），耗尽即沉底；1 关不限 |
| 重复命中衰减 | 仅第 6 关：同一弹珠对同一怪的重复命中 ×0.85／次，下限 40% |
| 弹珠数量 | `min(38, 12 + 2×(wave - 1) + fishBonus)`，鱼罐加成上限 8 |
| 基础伤害 | `1 + floor((wave - 1) / 3) + hunterBuff` |
| 普通敌人生命 | `(1.2 + 关卡hpSlope×wave)` 经取整后乘 0.75—1.20 的随机系数；爆米花怪再降低 |
| Boss 生命 | `关卡bossHP×wave`；无尽另乘下述压力系数 |
| 无尽额外压力 | 18 波后乘 `(1 + (wave-18)/12)^1.5`，防止线性成长抵消所有危险 |
| 速度与碰撞 | 640 逻辑像素／秒；120Hz 固定步长；圆对轴对齐矩形 |
| 发射间隔 | 0.066 秒；单轮最长 11 秒后自动回收 |
| 改装节奏 | 仅在斩杀大王时三选一（保卫战第 6／12／18 波，无尽每 6 波）；斩杀当轮免打烊压力。看广告可让大王奖励多选一项，或在瞄准阶段主动看广告换一次三选一；合计每局最多 3 次，广告接口当前为占位实现（`PlatformService.showRewardedAd`），不接真实 SDK、不产生广告请求 |
| 失败后奖励 | 有击破才获得印章；不操作后反复退出不给印章 |

消灭最后一波全部来敌即获胜，不再有额外的清场阶段。

## 店长天赋

三位店长开局各带 1 级专属改装；桃桃与芋圆另有天然天赋，不占用改装位：桃桃「开门红」——每轮第一次击杀必定爆炸（与开罐爆破叠加按改装等级结算）；芋圆「静电场」——每轮弹珠首次命中额外放出一道双目标闪电链。天赋不提供胜率豁免，只改变打法手感；第 1 关与每日同题强制薄荷，保证教学与同题公平。

## 十二种改装：规则与相互作用

| 改装 | 改变什么 | 等级上限 |
|---|---|---:|
| 跳跳雷链 | 每弹第 3 次接触触发传导；高等级改成第 2 次，增加邻居与范围 | 3 |
| 开罐爆破 | 普通击杀也爆炸；击杀引发邻居死亡可继续连锁 | 3 |
| 猫爪分裂 | 主弹首次击中分出 2／3／4 小弹；小弹不能递归分裂 | 3 |
| 糯米穿心 | 前 1／2／3 次命中不反弹；与分裂同时拥有时小弹继承一次 | 3 |
| 借墙蓄力 | 每次侧墙碰撞增加下一次命中的伤害份数，达到蓄力上限后不再加 | 3 |
| 回头客 | 主弹回到底部额外反弹 1／2 次，而非无限循环 | 2 |
| 冰糖脆壳 | 首击裹糖，下一次弹珠／雷电击中破冰倍伤，不依赖额外冰冻计时条 | 3 |
| 清巷热线 | 每 12／9／6 次命中横扫触发点所在行 | 3 |
| 飞鱼烟花 | 每击杀 3／2／1 个单位发射一对小弹 | 3 |
| 喵爪护盾 | 单轮达到 35／25／18 连击获得一层护盾，每轮限一次 | 3 |
| 弱点铃铛 | 标记最靠近防线的敌人，先击破可强化本轮剩余弹珠 | 3 |
| 罐头开锁器 | 卸甲并触发传导；高等级可在 Boss 首次接触触发 | 2 |

雷链＋冰糖可跨目标破冰；分裂＋穿透增加钻背机会；爆破＋横扫提供击杀链；回弹＋侧墙可反复蓄力。配方提示不额外发放神秘百分比，只解释已有系统交互。

有些高等级同时提升范围／伤害，属于强化已有行为，不以无限 +5% 列表替代机制。每项有明确上限。若改装池仅剩一两项，使用生命／弹珠／护盾补齐三个不重复选择。

## 随机、公平与策略

地图与抽牌使用带显式状态的 Mulberry32。粒子抖动可用独立的视觉随机，**不得影响战斗 RNG**。初始棋盘固定，使新手有稳定的教学反馈；之后随机列、类型、血量与改装带来差异。

每三波检查一枚炸弹的补偿机会，避免长期无连锁支点。能抽到未拥有机制时，三选一保证至少有一个新机制。全局可换一批一次，也可放弃本次构筑恢复 1 心。

选择不只在升级：优先救快越线的目标，或利用炸弹消除邻居；直射集火，或借墙获得蓄力并绕背；面对铁罐／大王是否优先选穿透、开锁或防守。数值代理显示直线策略弱于角度策略，但不能据此宣布不存在最优策略。

## 局外系统与无聊审查

所有 12 种机制从第一局就可出现，不把核心乐趣锁在长时间养成后。结算印章解锁店长的**初始机制**，不是永久提高攻击百分比。8 印章获得爆破开局的桃桃，20 印章获得雷链开局的芋圆；初始薄荷是侧墙蓄力。

配方发现、纪念章、本机记录、同种子再挑战和每日固定题提供第二局理由。完成含 Boss 击破的一局解锁无尽夜班。没有联机竞技或云端排名。第 1 局足够完整，但三小时之后是否还愿意玩没有实测；长期内容深度是保留风险。

## 性能预算与反软锁

同屏弹珠 112、单轮最多生成 180、敌人 58、粒子 220、视觉效果 64、飘字 24、事件队列 512、单 tick 连锁结算 160。DPR 上限 2，避免高像素密度手机过度绘制。所有上限在代码中实际执行。

弹珠近水平时加入微小纵向速度；飞行超过 11 秒自动回收；页面隐藏暂停，恢复不自动代打。连锁伤害使用有预算的队列，不递归无限调用。动画和声音可关闭／减弱；没有为 Hit Stop 强行更改回合物理，采用闪白、粒子、震动、连击音阶替代。

## 下一步迭代优先级

先做真实首次体验观察，修理看不懂、瞄不准、不能继续等阻塞；再验证两三条构筑的行为差异是否被玩家感知；最后才考虑新敌人和日题限制。暂不添加服务器、商店广告、装备仓库或大量手工关卡。
